/**
 * Application routes, relative to `baseURL` from playwright.config.ts.
 * Single source of truth for Page Object navigation and URL assertions.
 */
export const ROUTES = {
  home: '/',
  authors: '/authors',
} as const;
